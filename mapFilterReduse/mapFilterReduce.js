// ===========================mapFilterReduce======================

// if you want to make one other array related to first array ,
// with some specific changes.

let arr = [1, 2, 3, 4, 5, 9, 10];
console.log(arr);
for(let i = 0; i < arr.length; i++) {

    console.log(i);
}